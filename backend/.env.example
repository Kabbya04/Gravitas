# Copy to .env in this directory. Never commit .env.

# Required for draft generation (server-side only).
GROQ_API_KEY=

# Optional: overrides config.yaml groq.model when set.
# GROQ_MODEL=llama-3.3-70b-versatile

# Optional: absolute path for data directory (defaults to backend/data).
# DATA_DIR=
